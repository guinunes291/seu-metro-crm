/**
 * BI Sync Job — DESATIVADO por decisão do usuário para reduzir custos de Cloud.
 * Stubs mantidos para compatibilidade com importações existentes.
 * A sincronização BI pode ser acionada manualmente via procedure admin.
 */

export function startBISyncJob() {
  console.log("[BI Sync Job] Job automático DESATIVADO — sincronização apenas manual.");
}

export function stopBISyncJob() {
  // noop
}
